spring:
  application:
    name: mock-sales-system
  threads:
    virtual:
      enabled: true

server:
  port: ${SERVER_PORT:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
